'use strict';

// PUBLISH DI MODULE
module.exports = {
  'preprocessor:fixtures': ['factory', function() {
    var f = require('util').format
    var TEMPLATE = 'try { __FIXTURES__.push(%s) } catch(e) { var __FIXTURES__ = [];  __FIXTURES__.push(%s) }'

    return function(content, file, done) {
      file.path += '.js'

      done(f(TEMPLATE, content, content))
    }
  }]
}